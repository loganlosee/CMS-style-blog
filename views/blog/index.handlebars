{{#extend "layouts/main"}}
  {{#content "body"}}
    <h1>Blog Posts</h1>
    {{#each posts}}
      <div>
        <h2>{{title}}</h2>
        <p>{{content}}</p>
      </div>
    {{/each}}
  {{/content}}
{{/extend}}
